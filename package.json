{
  "name": "bbisping-academic",
  "version": "1.0.0",
  "description": "Site with works by Benjamin Bisping",
  "dependencies": {
    "@fontsource/poppins": "^4.5.10",
    "@fortawesome/fontawesome-free": "^6.2.0",
    "bootstrap": "^5.2.2",
    "d3": "^7.6.1"
  },
  "scripts": {
    "ruby-deps": "gem install bundler && bundle install",
    "dev": "bundle exec jekyll serve --trace --livereload --verbose",
    "build": "bundle exec jekyll build"
  }
}
